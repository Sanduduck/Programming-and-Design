# 데드코드 / 더미코드 정리 작업

전체 소스(56개 파일) 통독 + 심볼 사용처 교차 검증 결과. **삭제는 항목별 승인 후 진행.**
삭제 시 `Survice ICE.vcxproj` + `.filters` 등록 해제도 함께 처리.

## 핵심 전제
`init_game()`은 [game_state.c:14-17](src/game_state.c#L14-L17)에서 **`init_settings()` + `init_player()`만** 호출.
아래 A그룹(공용 장애물/패턴/충돌/점수 프레임워크)은 **어디에서도 초기화·호출되지 않음.**
실제 게임은 phase1/2/3 모듈이 각자 장애물·렌더·충돌을 자체 구현하고 `player_damage()`를 직접 호출하는 구조 → 공용 프레임워크는 버려진 초기 스켈레톤.

---

## A. 완전히 끊긴 공용 프레임워크 (확실한 데드코드)

- [ ] **① pattern.c / pattern.h** — `init_patterns`, `start_pattern`, `update_pattern`, `is_pattern_finished`, `is_phase_finished`, `next_phase`, 전역 `current_phase`/`current_pattern_id`.
  - 전부 본문이 `// TODO` 빈 껍데기. 호출하는 곳 0곳. 실제 페이즈 시스템은 phase1.c/phase2.c/phase3.c로 완전히 별개. **가장 확실한 삭제 후보.**

- [ ] **② obstacle.c / obstacle.h** — `obstacles[]`, `init_obstacles`, `spawn_obstacle`, `update_obstacles`, `draw_obstacles`.
  - `spawn/update/draw_obstacles` 어디서도 안 불림. `update_obstacles`는 `(void)dt;` TODO 스텁. 참조하는 건 죽은 pattern.c/collision.c/score.c뿐.

- [ ] **③ collision.c / collision.h** — `check_collision`, `check_all_collisions`.
  - `check_all_collisions` 호출 0곳. (phase3/ml.c의 `check_collision`은 동명이인 static 함수로 무관, 그건 살아있음.)

- [ ] **types.h 구조체 정리** — A그룹 삭제 시 [types.h:31-48](src/types.h#L31-L48)의 `Obstacle`(32-39)·`Pattern`(42-48) 구조체도 참조처 사라짐 → 같이 정리 가능.

---

## B. 미래 기능용 스캐폴딩 (지금은 데드, CLAUDE.md상 "구현 예정" — 지우면 나중에 재작성)

- [ ] **④ score.c / score.h** — `init_score`, `add_score`, `check_score_for_obstacles`, `calc_grade`, `total_score`.
  - 현재 호출처 0곳. 단 `calc_grade`/`total_score`는 구현순서 6·7번(점수·등급·결과화면)에서 사용 예정. `check_score_for_obstacles`는 죽은 `obstacles[]` 기반이라 A그룹과 운명 공유.

- [ ] **⑤ settings_effective_bgm() / settings_effective_sfx()** — [settings.c:28-34](src/settings.c#L28-L34).
  - 호출처 0곳 (CLAUDE.md도 "호출하는 쪽이 없음" 명시). 구현순서 10번 사운드 모듈용 예약.

- [ ] **⑥ phaseN_current() 6개** — [phase1.c:53](src/phase1.c#L53), [phase2.c:53](src/phase2.c#L53), [phase3.c:53](src/phase3.c#L53).
  - 정의만 있고 호출 0곳.

- [ ] **⑦ phaseN_pattern_finished() 3개** — [phase1.c:43](src/phase1.c#L43), [phase2.c:43](src/phase2.c#L43), [phase3.c:43](src/phase3.c#L43).
  - 호출 0곳. "패턴 끝나면 다음으로" 진행 로직 미구현이라 미사용. (※ 각 과목의 `*_finished()` 리프 함수는 이 함수가 호출하므로 **살아있음 — 지우면 안 됨.**)

---

## 데드코드 아님 (지우지 말 것)
- phase1/2/3 모든 과목 모듈 — 정상 동작.
- ui_result.c(검은 배경만 그림), ui_ranking.c의 `ranking[]`(파일 로드 미연결로 0점 10줄 표시) — **"미완성 기능"이지 데드코드 아님.** 구현순서 7·8번.

---

## 진행 상태
- 보고 완료 / 삭제 대기 중. 사용자가 항목 번호로 승인 시 해당 항목만 삭제 + VS 프로젝트 등록 해제.
