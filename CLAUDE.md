# 정통에서 살아남기 (Survive in IT)

성결대학교 정보통신공학과 요소 기반 2D 패턴 회피 게임. C 언어 기반 팀 프로젝트.

---

## 역할 및 작업 원칙

- C 언어 기반 게임 개발 보조.
- **요청된 기능만 구현. 임의로 범위 확장 금지.**
- 요구사항이 불분명하면 추측하지 말고 먼저 질문할 것.
- 수업 규칙 위반 요청 시: 위반 항목을 먼저 알리고 규칙 내 대안을 제시.
- 구현은 단계별 순차 진행. 단계 건너뛰지 말 것.

---

## 수업 규칙 (절대 준수)

| 항목 | 허용 여부 |
|------|----------|
| 주 언어 C | 기본 |
| Win API (C++) | 사운드/마우스/알림창 한정 허용 |
| SDL2 / SDL2_image / SDL2_ttf | 허용 (교수 확인 완료) |
| GUI 기반 C++/MFC | **불가** |
| 키보드 입력 | 기본 |
| Win API 마우스 위치 읽기 | 허용 |
| `printf()`만으로 구성된 출력 | 저평가 — 피할 것 |

평가 기준: 정교한 기능 + 탄탄한 알고리즘 논리.

---

## 게임 개요

- **장르**: 화면 고정형 2D 패턴 회피 (횡스크롤 아님)
- **그래픽**: SDL2 스프라이트 렌더링, **시안(Cyan) + 다크 배경** 테마
- **사운드**: Win API (`PlaySound` / `mciSendString`) — BGM + 효과음
- **컨셉**: 정통과 학생이 1→4학년 학년별 교수님 전공 과목 요소 패턴을 회피하며 졸업(클리어)

### 종료 조건
1. HP 0 → 게임 오버 → 결과 화면
2. Phase 4 통과 → 졸업 클리어 → 결과 화면

두 경우 모두 점수 + 등급을 `ranking.txt`에 자동 저장.

---

## 화면(상태) 구성

### 상태 머신
```
MENU ──┬─→ PLAYING ─→ RESULT ─┬─→ PLAYING (재시작)
       │                       └─→ MENU
       ├─→ RANKING ─→ MENU
       └─→ SETTINGS ─→ MENU
```

### 메인 메뉴
시작 / 랭킹 / 설정 — 3개 항목.

### 랭킹 화면
`ranking.txt`에서 점수 읽어 **내림차순 정렬, 상위 10개** 표시 (점수 + 등급). 메뉴로 복귀 키 제공.

### 설정 화면
- 볼륨 조절: BGM / 효과음 **개별 조절**
- 키 커스터마이징: 좌(←), 우(→), 점프(↑) — 동작 선택 후 키 직접 입력으로 변경
- 변경 키 설정은 게임 진행 중 유지

### 플레이 화면
화면 고정 영역 내 플레이어 + 장애물. UI: 보스(교수님 아스키 아트), HP, 점수, 페이즈.

### 결과 화면
최종 점수 + 등급 표시. **Win API 마우스 클릭 또는 키 입력**으로 즉시 PLAYING 재시작 또는 MENU 복귀.

### 등급
| 등급 | GPA |
|------|-----|
| A+ | 4.5 |
| A0 | 4.0 |
| B+ | 3.5 |
| B0 | 3.0 |
| C+ | 2.5 |
| C0 | 2.0 |
| D+ | 1.5 |
| D0 | 1.0 |
| F  | 0.0 |

구간별 점수 기준은 **팀 협의 후 확정** — 임의로 정하지 말고 확정 시 코드에 반영.

---

## 플레이어

- **조작**: 좌우 이동 + 점프 (2단 점프), 키 커스터마이징 가능, 기본 방향키
- **물리**: 중력 적용 포물선 점프
- **HP**: 4 (고정)
- **피격 시**: 일정 시간 무적 + 깜빡임 시각 효과
- **힐팩**: 특정 패턴 종료 시 등장, 획득 시 HP +1

---

## 점수 시스템

- **장애물별 고정 점수.** 각 장애물(또는 패턴 요소)마다 자체 점수값을 가지고, 회피 성공(화면 밖으로 미충돌 통과) 시 그 점수를 누적.
- 시간 기반 자동 증가 **없음**
- 회피 성공 보너스 **없음** (회피 자체가 점수 부여 트리거이므로 별도 보너스 개념 불필요)
- 피격 보너스 **없음**
- 콤보 시스템 **없음**
- 종료 시 최종 점수로 등급 산출

---

## 페이즈 (Phase 1~4)

학년제 구조. 각 페이즈는 해당 학년 담당 교수님 + 전공 과목 요소 기반.

### 보스 (교수님)
- 학과 홈페이지 사진 → **사전 동의 확보 후** 아스키 아트 텍스트로 변환
- 저장 위치: `assets/ascii_art/` (페이즈별 파일)
- `stdio.h` 파일 I/O로 로드 → SDL2_ttf **고정폭 폰트**로 렌더링
- 담당 교수 / 세부 패턴은 섭외 + 사전 동의 확보 후 확정

### 패턴 설계 원칙
- 해당 교수 전공 과목 요소를 시각적으로 연상 가능한 형태
- 이동 경로 / 출현 타이밍 / 조합으로 다양성 확보
- **좌우 이동 + 점프만으로 모든 패턴 회피 가능해야 함**
- 페이즈 진행될수록 난이도 상승
- 특정 패턴 종료 시 힐팩 등장 (HP +1 기회)

---

## 사운드

- 헤더: `windows.h`, `mmsystem.h`
- 링크: `winmm.lib`
- BGM: 페이즈별 — `mciSendString` 또는 `PlaySound`
- 효과음: 상황별 모두 재생
- 볼륨: 설정 화면에서 BGM/효과음 개별 제어

---

## 랭킹

- 게임 종료 시 점수 + 등급을 `ranking.txt`에 저장
- 상위 10개 유지, 내림차순 정렬
- 랭킹 화면에서 점수 + 등급 함께 표시

---

## 사용 라이브러리

| 라이브러리 | 용도 |
|-----------|------|
| SDL2 | 윈도우, 렌더링, 입력, 타이머 |
| SDL2_image | PNG 스프라이트 로드 |
| SDL2_ttf | UI 텍스트 + 아스키 아트 (고정폭 폰트) |
| stdio.h | 파일 I/O (랭킹, 설정, 아스키 아트) |
| stdlib.h | 랜덤, 메모리 |
| time.h | 시드, 타이머 |
| windows.h | Win API (마우스, 알림창) |
| mmsystem.h | Win API 사운드 |
| winmm.lib | 사운드 링크 |

---

## 프로젝트 구조 (실제)

```
C_Programming/
├── src/
│   ├── main.c
│   ├── game_state.c / game_state.h    # 메인 루프, 상태 관리
│   ├── player.c / player.h            # 플레이어 로직, 키 커스터마이징
│   ├── obstacle.c / obstacle.h        # 장애물 로직 (공용 사각 장애물)
│   ├── pattern.c / pattern.h          # 페이즈 전체 진행 관리 (스켈레톤)
│   ├── collision.c / collision.h      # 충돌 판정 (공용 AABB)
│   ├── score.c / score.h              # 점수 + 등급 산출
│   ├── types.h                        # 공용 타입 정의
│   ├── phase3.c / phase3.h            # 3학년 패턴 디스패처
│   ├── phase3/                        # 3학년 패턴 서브 모듈
│   │   ├── blockchain.c / .h          # 블록체인 (김도규)
│   │   ├── os.c / .h                  # 운영체제 (김도규) — 스켈레톤
│   │   ├── ml.c / .h                  # 머신러닝 (김인겸) — 스켈레톤
│   │   └── webserver.c / .h           # 웹서버 (정복래) — 스켈레톤
│   ├── ui_menu.c / ui_menu.h          # 메인 메뉴 화면
│   ├── ui_play.c / ui_play.h          # 플레이 화면
│   ├── ui_ranking.c / ui_ranking.h    # 랭킹 화면
│   ├── ui_result.c / ui_result.h      # 결과 화면
│   └── ui_settings.c / ui_settings.h  # 설정 화면
├── assets/
│   ├── ascii_art/                     # 페이즈별 교수님 아스키 아트
│   ├── fonts/                         # 고정폭 폰트 (SDL2_ttf용)
│   ├── patterns/                      # 패턴 데이터
│   └── sounds/                        # BGM + 효과음
├── SDL2-2.30.12/
├── SDL2_ttf-2.22.0/
├── Survice ICE/                       # Visual Studio 프로젝트 폴더
├── ranking.txt                        # 랭킹 기록
└── README.md
```

### 명세 대비 차이
- `game.c/h` → `game_state.c/h` (실제 명칭)
- `collision.c/h` 추가됨 (충돌 분리)
- `render.c/h` 대신 화면별 `ui_*.c/h`로 분리
- 아직 미생성: `ranking.c/h`, `sound.c/h`, `settings.c/h`
- assets: `images/` 대신 스프라이트는 `patterns/` 등에 배치, `ascii/` → `ascii_art/`, `fonts/` 추가

> **새 파일 생성 시**: 위 실제 구조의 명명 규칙(스네이크 케이스, `ui_` prefix 등)을 따를 것.

---

## 구현 순서 (팀 합의 단계)

1. SDL2 초기화 + 윈도우 생성
2. 메인 메뉴 (시작/랭킹/설정)
3. 플레이어 이동 / 점프 / 중력
4. 장애물 생성 / 이동 / 충돌
5. 무적 / 깜빡임 시스템
6. 점수 시스템 + 힐팩 지급
7. 등급 산출 + 결과 화면 (클릭/키 재시작)
8. 랭킹 파일 I/O
9. 설정 화면 (볼륨 + 키 커스터마이징)
10. Win API 사운드 (BGM + 효과음)
11. 페이즈별 보스 패턴 (Phase 1~4 순차)

> **현재 단계가 어디인지 확인 후 작업할 것.** 앞 단계가 미완이면 점프하지 말고 먼저 점검.

---

## Phase 3 (3학년) 진행 상황

### 모듈 구조
- `src/phase3.c/h` — 디스패처. `Phase3PatternId` enum으로 활성 패턴 선택, start/update/draw/finished를 서브 모듈로 위임.
- `src/phase3/<subject>.c/h` — 각 과목별 패턴 모듈. 외부 노출 API: `<subject>_start/update/draw/finished`.

### 패턴 모듈 설계 원칙
- **각 패턴은 자체 인스턴스 배열 + 자체 렌더 + 자체 충돌 판정을 보유.** 공용 `obstacle.c` (사각 장애물)와 분리 — 비트코인 같이 비-사각형 시각이 필요하기 때문.
- 충돌 시 `player_damage()` 호출만 하면 됨. **무적 처리는 `player_damage` 내부에서 `invincible_timer`로 처리되므로 패턴 측에서 별도 `damaged` 플래그 불필요.**

### 과목별 상태
| 패턴 | 담당 교수 | 상태 | 비고 |
|------|----------|------|------|
| 블록체인 | 김도규 | **완료** | 비트코인 16개, ~7초 시퀀스 |
| 운영체제 | 김도규 | 스켈레톤 | 패턴 설계 미정 |
| 머신러닝 | 김인겸 | 스켈레톤 | 패턴 설계 미정 |
| 웹서버프로그래밍 | 정복래 | 스켈레톤 | 패턴 설계 미정 |

### 블록체인 패턴 세부
- 비트코인 동전: 노란 황금색(247,197,36) 채워진 원 + 검정 ₿ (좌측 spine + 가로 3획 + 우측 상하 막대 + 위/아래 수직 돌출)
- 원은 수평 스캔라인(`SDL_RenderDrawLine` 반복)으로 채움 — `math.h`의 `sqrtf` 사용
- 충돌: 원-사각 근사 (가장 가까운 모서리 점과의 거리 ≤ r)
- 스폰: `BitcoinSpawn` 정적 배열 (spawn_time, x, radius, vy0). `pattern_time` 누적 비교로 트리거.
- 낙하 가속도: `FALL_GRAVITY = 240.0f` (px/s²)
- 종료 조건: 모든 스폰 소진 + 활성 비트코인 0

### ui_play.c 통합 (테스트용)
- 플레이 진입 시 자동으로 `PHASE3_BLOCKCHAIN` 시작
- 핫키: `1` 블록체인 / `2` OS / `3` ML / `4` 웹서버 — 즉시 전환 (개발용)

### Visual Studio 프로젝트
- `Survice ICE.vcxproj` + `.filters` 양쪽에 phase3 관련 파일 모두 등록 완료
- Solution Explorer에서 `소스 파일\phase3`, `헤더 파일\phase3` 하위 필터로 묶임
- **새 .c/.h 추가 시**: `.vcxproj`(`<ClCompile>`/`<ClInclude>`) + `.filters`(필터 지정) 양쪽 수정 필요. 빠뜨리면 VS가 빌드 대상으로 인식 못함.

### 다음 작업 후보
- OS / ML / 웹서버 패턴 설계 → 구현
- 블록체인 패턴 데이터 튜닝 (스폰 타이밍/크기 분포) — 실제 플레이로 난이도 검증 필요
- 패턴 종료 시 힐팩 등장 (`drop_heal_item` 활용) — 미구현

---

## 코드 작성 규칙

- 들여쓰기: 기존 파일 스타일 따를 것 (확인 후 맞추기)
- 명명: 함수/변수 `snake_case`, 매크로 `UPPER_SNAKE`, 타입 `PascalCase` 또는 `_t` suffix — 기존 `types.h` 확인 후 통일
- 주석: WHY가 비자명할 때만. 코드 설명 주석은 작성 금지.
- printf 디버깅 남발 금지 (평가 항목)
- 헤더 가드 또는 `#pragma once` 일관성 유지
- 한글 문자열은 UTF-8 또는 SDL2_ttf 호환 인코딩 확인 필요

---

## 외부 의존 / 미확정 사항

작업 전 확인할 것:
- [ ] 등급 점수 구간 (팀 협의 미확정)
- [ ] 페이즈별 담당 교수 (섭외/사전 동의 미확정)
- [ ] 페이즈별 세부 패턴 (확정 후 코딩)
- [ ] 교수 사진 아스키 아트 변환 (사전 동의 후)

> 위 항목 관련 코드 요청 시: 확정 여부를 먼저 확인하고, 미확정이면 placeholder/TODO로 처리하고 사용자에게 알릴 것.

---

## 기본 구성 미구현 사항

게임이 정상적으로 굴러가려면 반드시 들어가야 하는 기본 파트 중 아직 비어 있는 항목. 위 「구현 순서」 단계 번호와 매칭.

### 6. 점수 + 힐팩
- [ ] **힐팩 등장 / 획득 / HP +1** — `Pattern.drop_heal_item` 플래그만 정의돼 있고 스폰·렌더·획득 로직 없음. 패턴 종료 타이밍에 맞춰 트리거.
- [ ] **장애물별 점수 부여 연결** — `score.c`의 `add_score(10)` 하드코딩 제거. 장애물별 고정 점수 모델이므로:
  - 패턴 내 모든 장애물이 동일 점수면 → `Pattern.score_per_obstacle` 사용 (현재 필드 그대로).
  - 장애물마다 점수가 다르면 → `Obstacle` 구조체에 `int score;` 필드 추가가 더 자연스러움.
  - **선택은 패턴 설계 확정 후 결정.** 패턴 모듈(예: 블록체인)이 자체 장애물을 갖는 구조이므로, 공용 `Obstacle`에 필드를 넣더라도 각 패턴이 자기 인스턴스에 점수값을 따로 들고 있어야 함.

### 7. 결과 화면
- [ ] **점수 + 등급 표시** — 현재 `ui_result.c`는 검은 배경만 그림. `total_score` + `calc_grade` 결과를 텍스트로 렌더링해야 함.
- [ ] **마우스 클릭 재시작 / 메뉴 복귀** — Win API 마우스 클릭으로 즉시 재시작. 현재는 `Enter` 재시작 / `ESC` 메뉴만.

### 8. 랭킹 파일 I/O
- [ ] **`ranking.c/h` 모듈 자체 미생성** — `ranking.txt` 읽기/쓰기, 상위 10개 정렬 유지.
- [ ] **`ui_ranking.c`와 연결** — 현재 정적 배열만 표시. 게임 종료 시 점수+등급 자동 저장도 미연결.

### 9. 설정 화면
- [ ] **설정값 영속화 (`settings.txt`)** — 현재 `g_settings`는 시작 시 항상 기본값. 랭킹 파일 I/O 작업과 묶어서 처리하면 깔끔.

### 10. Win API 사운드
- [ ] **`sound.c/h` 모듈 자체 미생성** — `windows.h` + `mmsystem.h` + `winmm.lib`로 `PlaySound` / `mciSendString` 래핑.
- [ ] **실효 볼륨 연결** — `settings_effective_bgm/sfx()` 헬퍼만 있고 호출하는 쪽이 없음. 사운드 모듈 안에서 호출.
- [ ] **페이즈별 BGM + 상황별 효과음** — 점프/피격/힐/패턴 종료 등.

### 11. 페이즈별 보스 패턴
- [ ] **Phase 1 / 2 / 4** — 미착수.
- [ ] **Phase 3 OS / ML / 웹서버** — 스켈레톤 상태, 패턴 설계 미정.
- [ ] **블록체인 패턴 종료 시 힐팩 트리거** — 위 6번과 연결.

> 이 목록의 항목을 한 번에 처리하지 말 것. 사용자 요청 단위로 하나씩 진행.
